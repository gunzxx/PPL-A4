<link rel="stylesheet" href="/css/uiv/button1.css">
<div class="scene">
  <div class="cube">
    <span class="side top">{{ isset($top) ? $top : "Easter" }}</span>
    <span class="side front">{{ isset($front) ? $front : "Easter" }}</span>
  </div>
</div>